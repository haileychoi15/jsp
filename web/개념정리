서블릿 => 자바 웹 (서버) 프로그램
            |
            | (웹이 담당하고 있는 부분)
            | (콘솔 프로그램은 system.in and out 을 통해 입출력)
            | (웹기반은 서블릿코드가 제공하는 request and response를 통해 입출력)
            |
           웹에 입출력 => request : 입력 설정(UTF-8), 입력값 읽기
                       response : 출력 설정, 출력스트림 => 웹문서 출력(html을 출력하는데 여러 문제 발생, 단순 문자열 출력을 원하는것이 아니기 때문)
                                                        |
                                                        |
                                                      문서 기반(jsp)의 코드블록 (도와주려고 생기긴 했는데 코드블록 많아지고 유지관리 어려워짐)
                                                        |
                                                        |
                                                      MVC 방식으로 나눠서 코드를 만들기로 했다.
                                                        |
                                                        |
                                                      VIEW단에서 코드블록이 필요한 경우? => EL : 값을 출력해주는 역할
                                                                                    JSTL : 특정 출력 영역을 제어하는 역할(tag library 형태)

